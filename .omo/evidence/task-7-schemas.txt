TASK 7: schemas.py engine_version 扩展
========================================

## 1. 改动前 grep（所有 v1/v2 出现处）

$ git show HEAD:backend/app/models/schemas.py | grep -n "v1\|v2\|Literal"
7:from typing import Any, Literal
85:    v1 = "v1"
86:    v2 = "v2"
172:    engine_version: EngineVersion = EngineVersion.v1
176:    # 情绪控制（v2 only）
180:    # v2 直接情绪名（优先级高于 emotion_mode）
181:    emotion: Literal["happy", "sad", "angry", "afraid", "disgusted", "melancholic", "surprised", "calm"] | None = Field(default=None, description="v2 direct emotion name")
195:    # v2 专属
225:    engine_version: str = "v1"
246:    engine_version: str = "v1"
262:    default_engine_version: str = "v1"
274:    # v2 emotion defaults

## 2. git diff

$ git diff backend/app/models/schemas.py
diff --git a/backend/app/models/schemas.py b/backend/app/models/schemas.py
index 2775310..6344e40 100644
--- a/backend/app/models/schemas.py
+++ b/backend/app/models/schemas.py
@@ -82,8 +82,9 @@ class SegmentStatus(str, Enum):
 
 
 class EngineVersion(str, Enum):
-    v1 = "v1"
-    v2 = "v2"
+    indextts_v1 = "indextts-v1"
+    indextts = "indextts"
+    omnivoice = "omnivoice"
 
 
 # ── Emotion ────────────────────────────────────────────
@@ -169,16 +170,18 @@ class VoiceAsset(BaseModel):
 class GenerateRequest(BaseModel):
     text: str
     engine_id: str = "indextts"
-    engine_version: EngineVersion = EngineVersion.v1
+    engine_version: EngineVersion = EngineVersion.indextts
     voice_id: str | None = None
     reference_audio_path: str | None = None
+    ref_audio_path: str | None = None
+    ref_text: str | None = None
     language: str = "zh"
     # 情绪控制（v2 only）
     emotion_mode: EmotionMode = EmotionMode.follow_reference
     emotion_values: dict[str, float] | None = None
     emotion_text: str | None = None
     # v2 直接情绪名（优先级高于 emotion_mode）
-    emotion: Literal["happy", "sad", "angry", "afraid", "disgusted", "melancholic", "surprised", "calm"] | None = Field(default=None, description="v2 direct emotion name")
+    emotion: str | None = Field(default=None, description="Direct emotion name")
     emo_alpha: float = Field(default=0.6, ge=0.0, le=0.8, description="Emotion intensity 0.0-0.8")
     # 基础参数
     speed: float = Field(default=1.0, ge=0.5, le=3.0)
@@ -222,7 +225,7 @@ class GenerationTask(BaseModel):
     task_id: str
     task_type: str = "single"
     engine_id: str
-    engine_version: str = "v1"
+    engine_version: str = "indextts"
     voice_id: str | None = None
     input_text: str
     status: TaskStatus = TaskStatus.pending
@@ -243,7 +246,7 @@ class HistoryItem(BaseModel):
     result_id: str
     task_id: str
     engine_id: str
-    engine_version: str = "v1"
+    engine_version: str = "indextts"
     voice_id: str | None = None
     voice_name: str | None = None
     input_text: str
@@ -259,7 +262,7 @@ class HistoryItem(BaseModel):
 
 class AppSettings(BaseModel):
     default_engine_id: str = "indextts"
-    default_engine_version: str = "v1"
+    default_engine_version: str = "indextts"
     default_language: str = "zh"
     default_output_format: str = "wav"
     model_dir: str = "~/VoiceStudio/models"

## 3. import 验证

$ python -c "from backend.app.models.schemas import *"
（无输出 = 导入成功）

## 4. 三个引擎值构造验证

$ python -c "
from backend.app.models.schemas import GenerateRequest, EngineVersion

# Test indextts-v1
r1 = GenerateRequest(text='hello', engine_version=EngineVersion.indextts_v1)
assert str(r1.engine_version) == 'indextts-v1'
assert r1.ref_audio_path is None
assert r1.ref_text is None

# Test indextts (default)
r2 = GenerateRequest(text='hello')
assert str(r2.engine_version) == 'indextts'

# Test omnivoice
r3 = GenerateRequest(text='hi', engine_version=EngineVersion.omnivoice)
assert str(r3.engine_version) == 'omnivoice'

# model_dump 包含全部新字段
dump = r3.model_dump()
assert 'ref_audio_path' in dump
assert 'ref_text' in dump
assert 'emotion' in dump

print('ALL PASS ✓')
"
ALL PASS ✓
