============================================================
TASK-1: PyTorch/MLX 共存风险验证报告
============================================================
计划: 002-three-engines-complete | Wave: 1 | 任务: T1 (BLOCKING)
执行日期: 2026-06-06
执行方式: uv (Python 3.10, Apple Silicon)

============================================================
1. 当前环境状态
============================================================
- Python: 3.10 (venv: .venv/)
- MLX:      0.31.1 (mlx-metal 后端, GPU device)
- PyTorch:  2.10.0 (MPS available: True, MPS built: True)
- numpy:    2.2.6
- torchaudio: 2.10.0
- transformers: 5.3.0
- omnivoice: 0.1.5 (已安装于同一 venv)
- uv.lock:  125 packages, 锁定稳定, 无冲突

MLX 和 PyTorch 均能正常使用各自 GPU 后端:
  - MLX: 默认设备 Device(gpu, 0), matmul 正常
  - PyTorch: MPS available, matmul 正常

============================================================
2. OmniVoice 0.1.5 依赖约束
============================================================
必需依赖:
  numpy         (无版本约束)
  torch>=2.4
  torchaudio>=2.4
  transformers>=5.3.0
  accelerate
  librosa
  soundfile
  gradio
  pydub
  tensorboardx
  webdataset

可选依赖 (eval):
  funasr, jiwer==3.1.0, s3prl, unidecode, zhconv, zhon

============================================================
3. 冲突分析
============================================================
a) numpy:  OmniVoice 无版本约束, 当前 numpy 2.2.6 正常工作
b) torch:  OmniVoice 要求 torch>=2.4, 当前 2.10.0 ✓
c) mlx:    OmniVoice 不依赖 MLX, 无冲突
d) numpy 跨框架兼容性:
   - MLX 使用 numpy 2.2.6 ✓
   - OmniVoice (via torch) 使用 numpy 2.2.6 ✓
   - 无降级需求
e) GPU 后端: MLX (metal) 和 PyTorch MPS 独立, 互不干扰 ✓

结论: 当前环境中零版本冲突

============================================================
4. pyproject.toml 对照
============================================================
当前主依赖:
  mlx>=0.18.0, numpy>=1.24.0, sentencepiece, soundfile, ...

可选依赖 (convert/v2):
  torch>=2.0.0, torchaudio>=2.0.0, transformers>=4.40.0

omnivoice 的约束 (torch>=2.4, transformers>=5.3.0)
比现有的 optional deps (torch>=2.0, transformers>=4.40) 更严格,
但当前已安装版本 (torch 2.10.0, transformers 5.3.0) 完全满足。

============================================================
5. 注意点
============================================================
- omnivoice 当前通过 pip install 安装 (不在 uv.lock 中)
- 如果要正式添加为依赖, 建议: uv add omnivoice
  (torch 2.10.0 / transformers 5.3.0 均已满足要求)
- gradio 是 omnivoice 依赖 (非必需推理时), 体积较大
- transformers 5.3.0 比当前 v2 extra 要求的 >=4.40.0 更新, 兼容

============================================================
★ VERDICT: GO
============================================================
无 numpy/torch/mlx-core 版本冲突。
MLX 0.31.1 与 PyTorch 2.10.0 在同一个 Python 3.10 环境中
完美共存, 两个 GPU 后端均可正常使用。

无需隔离 venv。可继续执行后续 T2/T3。
