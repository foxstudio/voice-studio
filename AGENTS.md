# Voice Studio 开发规则

本文件是仓库内所有 AI Agent 和开发者必须遵守的长期规则。它只记录稳定约束，不记录某次开发进度、聊天过程、测试数量或临时结论。

## 开始工作前

1. 先查看 Git 状态，确认当前分支和用户已有改动；不得覆盖无关修改。
2. 先读 `docs/architecture/README.md`，再按任务路由到对应领域说明和契约。
3. 修改前确认真实调用链、状态所有者、公共入口、兼容边界和已有测试。
4. 代码库调研、多文件排查、日志分析、测试验证、方案对比或独立审查，优先评估并委派边界清晰的子任务；主 Agent 负责整合与最终验证。
5. 读 `docs/DEV_STATUS.md`，了解当前焦点、已完成工作、关键经验和协作偏好。

## 总体架构

- Voice Studio 是模块化单体：WebUI、REST/OpenAPI、后台任务和外部 Agent 必须复用同一应用服务或领域门面。
- 同一业务能力只保留一套实现。页面、API 或队列不得各自复制算法、状态机或业务规则。
- 依赖方向保持单向：
  - `api` 负责传输、鉴权、错误映射和响应格式。
  - `application/service/workflow` 负责用例编排、事务与任务生命周期。
  - `domain` 负责业务规则、版本化输入输出和不变量。
  - `engine/provider` 隔离本地模型、云服务和第三方差异。
  - `store/repository` 负责持久化，不反向持有业务编排。
  - `frontend` 负责组合、交互和展示，不重新实现后端业务能力。

## 模块和类

- 每块大能力应有唯一、稳定、可测试的公开门面；存在生命周期、策略或一组相关操作时优先使用类，例如 ASR 管线。
- 不为“面向对象”强行创建空壳类。无状态、确定性的转换和校验应使用纯函数，并放在所属领域模块。
- 私有辅助只服务模块内部；跨模块复用必须经过明确公共接口，禁止导入内部实现细节。
- API 路由、后台队列和页面适配器只调用公开门面，不绕过它直接调用多套底层函数。

## 原子子任务和工作流

- 每个原子子任务必须有完整、版本化、可序列化的输入输出契约。
- 子任务需要明确：任务 ID、职责、依赖、执行方式、失败语义、输出版本和可观察结果。
- 没有数据依赖的任务默认并行；汇合必须建模为独立任务，并校验上游来源与指纹。
- 多项目先区分数据隔离与资源争用；共性瓶颈在唯一公共调度层解决，并覆盖任务选择、真实设备和模型生命周期。优先级只在安全任务边界生效；局部队列通过不代表全机并发通过。
- 一个子任务失败不得污染无关任务；允许降级时必须显式返回 warning、原因和可追溯结果。
- 正式流程、开发单步、OpenAPI 和 Agent 调用必须复用同一领域实现。
- 开发断点和正式流程可以有不同编排，但不能有不同算法。

## 公共契约与兼容

- 新增或修改可外部调用的能力时，优先提供 typed schema 和可读的 OpenAPI 文档。
- 公共契约不得以任意 `dict`、任意本地路径或隐含字段作为长期接口。
- API、数据库、任务负载、文件结构或真实数据结构变化，必须说明兼容策略并补回归测试。
- 旧任务只做读取时薄适配；不得用当前项目状态伪造旧任务当时未保存的结果。
- 迁移完成后删除旧路径，禁止两套实现长期并存。

## 状态与数据

- 区分项目持久数据、用户设置、任务状态、页面临时状态和高频播放状态。
- 每个模块只写自己拥有的状态；跨模块更新通过公开服务或事件完成。
- 高频播放位置、拖动状态和临时展开状态不得高频写入项目持久层。
- 文件系统能力集中在明确的媒体/存储模块；业务模块不散落拼接路径。
- 正式运行时只使用 `VOICE_STUDIO_DATA_DIR/config/voice_studio.db` 作为主数据库；
  迁移备份只能位于受管备份目录并保持只读语义，旧路径、仓库目录和系统临时目录
  不得成为第二个运行时数据库。测试数据库必须位于本次测试拥有的临时根目录。
- 不提交模型、音色、生成结果、数据库、缓存、备份、密钥或 `local-overrides/`。
- 不输出或记录 API Key、授权头、私人联系信息和未脱敏的本地绝对路径。

## 开发与测试临时数据

- 自动化测试、开发调试、浏览器验收和基准测试严禁写入用户正式数据根目录，
  尤其不得写入真实的 `config/`、`projects/`、`outputs/`、`exports/`、`assets/`
  或 `cache/`。启动被测服务或导入应用模块前，必须把数据库和所有可写存储路径
  一次性指向同一个由本次运行独占的外部临时根目录。
- 临时目录必须由统一生命周期管理：优先使用 pytest `tmp_path`、语言运行时的
  `TemporaryDirectory`，或带明确任务前缀的 `mktemp -d`。不得在仓库、产品数据
  目录或系统临时目录中留下无人负责的固定名称目录。
- 创建者负责清理。无论成功、失败、取消或超时，都必须在 `finally`、fixture
  teardown 或等价的会话清理钩子中删除本次产生的临时数据库、项目包、媒体切片、
  调试快照、日志和导出产物。需要暂时保留故障证据时，必须说明路径、用途和清理
  时点；任务收尾前仍应删除，除非用户明确要求保留。
- 用户交付文件只允许写入用户明确选择的导出目录或正式项目目录；开发产物不得
  混入用户交付目录。开发快照必须位于产品正式数据目录之外，并在验证完成后清理。
- 收尾前必须检查本次临时根目录和正式数据目录的新增项，确认没有测试项目、测试
  数据库、孤立缓存或调试导出残留。发现来源不明的现存文件时只报告和精确定位，
  未确认归属不得批量删除；禁止删除整个用户数据根目录或使用宽泛通配符清理。

## 前端

- 页面负责工作台编排；复杂状态下沉到 controller/store；可确定的几何、导航、映射和格式化下沉到共享纯 helper。
- 同一结果在时间线、播放器、侧栏和设置中必须使用同一状态来源，禁止多套字幕或播放状态。
- 组件共享真实结构，不复制“进行中/历史记录”等相同 DOM 和状态逻辑。
- 默认文案用大白话。普通层说明“做了什么、结果如何、需要看什么”；调试信息放在默认折叠的高级区域。
- 保持紧凑、自适应、响应式、键盘可操作、焦点可见，并尊重减少动画设置。
- 视觉调整遵循现有设计语言；除非任务明确要求，不用大规模换肤代替结构修复。
- 页面标题区用 `lib/components/PageHeader.svelte`；搜索框用 `app.css` 的 `.search-field`（紧凑场景加 `.compact`）与 `.search-clear`；筛选行用 `.toolbar-grid`。页面不得再自行定义这些控件。
- 样式优先写在组件的 `<style>` 里，由 Svelte 自动作用域隔离。确因主文件过大而拆到 `+page.css` 时必须知道：通过 `@import` 引入的样式**不带作用域，全局生效**，且不会被编译期检查未使用规则。因此这类样式文件的类名一律带页面前缀（如 `compose-`、`records-`），禁止定义 `.panel`、`.row`、`.card`、`.btn`、`.badge`、`.empty` 等通用名，避免静默覆盖其他页面。
- 新增页面元素前先查 `app.css` 和 `lib/components/`：相同用途的控件已有实现时复用，不新建第二套。

## 长流程与开发快照

### 开发调试模式决策门

- 对 ASR、本土化、从合成语音生成字幕以及其他由多个原子子任务组成的长流程，只要用户目标或当前语境包含排查、诊断、监控后发现异常、修复 Bug、升级逻辑、验证修复、失败后继续等含义，默认且必须使用 `development_target` 或该流程等价的开发调试模式。应先查找并沿用同项目、同流程已有的 `development_session_id` 和有效快照，从故障或受影响节点开始逐节点验证；不得因为页面上没有明显入口就假定开发调试能力不存在，也不得用正式全流程反复试错。
- 只有同时满足以下条件，才可以直接使用非开发模式：
  1. 用户明确要求正式端到端运行、最终交付，或明确要求执行某个正常生产步骤；
  2. 当前没有已知错误、失败任务、待验证修复或正在排查的异常；
  3. 该动作不是为了定位问题、验证修复或试探流程是否可用。
- 如果用户表述和当前环境不足以明确区分“正式生产执行”与“Bug 排查或修复验证”，必须在启动任何长流程或付费调用前请求澄清；得到用户明确回复后再执行，不得自行选择正式全流程。
- 开发调试按 DAG 依赖顺序逐节点验证：上游节点通过并保存版本化输入输出快照后，才推进其下游节点；某节点失败只重放该节点和真实受影响的后置校验。所有开发节点通过后，只有用户明确要求或任务本身明确包含最终交付时，才进行一次不读取开发快照的正式端到端验收。
- 故障节点修复并通过快照测试，不代表本轮开发调试已经完成。必须继续沿同一 `development_session_id`，按 DAG 顺序把该节点之后所有可达的后续节点逐个作为开发目标执行和验收；每一步复用已通过快照，遇到新故障就在该节点定点修复并从这里继续。只有后续节点全部通过，才允许启动最终正式端到端验收。不得把一次正式全流程运行当成后续开发节点逐项验收的替代品。

### 音轨默认值与用户确认门

- 视频本土化默认先完成人声/背景分离；源语言 ASR、说话人分析、时间对齐和后续真实性复核统一使用分离人声轨。`auto` 只表示在分离人声可用时选择它；分离人声缺失或失败时应先修复分离链路，不得静默回退到原音轨。
- 原音轨不是分离人声失败时的自动替代品。只有用户在本次任务中提前明确指定或在工具说明影响后再次明确同意，才可以把原音轨用于 ASR、对齐或复核；批准范围必须写入当前任务参数，不能从历史项目、页面默认值或 Agent 推断。
- 运行中若恢复方案会改变已经确认或默认锁定的音轨、识别/合成引擎、语言方向、云端/本地边界、付费路径、数据范围或最终交付效果，必须暂停受影响分支，用大白话说明“原方案、拟变更、影响”，等待用户明确确认后才能继续。不得先执行再补问。
- 已确认链路内部的重试、分块、提示词修复、纯本地代码修复、开发快照定点重放和后置校验不属于策略变更，应直接进入开发调试模式推进；但安装依赖、启用新的外部服务或新增付费调用仍按各自授权规则处理。

- 多子任务长流程调试时，在故障子任务入口保存产品正式数据目录之外的临时快照。
- 只重放受影响的子任务及后续校验；前序逻辑、输入结构或真实输入变化时才刷新快照。
- 正式任务默认从头计算，不读取开发快照；开发快照不得演变成未评审的产品缓存。
- 修复通过后，只有用户明确要求正式端到端验收，或任务本身明确包含最终交付时，才再跑一次不使用开发快照的完整正式路径；否则停在已通过的开发节点验证结果，不得自行追加正式全流程。
- 真实 ASR、LLM、搜索、识图和其他按量计费调用是开发测试的成本边界。除非正在验证该付费能力本身，不得为了页面刷新、文案调整、本地规则修改或无关回归重复调用。
- 付费原子任务进入分批执行前，开发调试应保存可重放的版本化批次输入、候选动作、输出和调用记录。某一批有问题时，默认只重放该批并复用其他已通过批次；只有提示词、输入契约、候选生成或全局汇合规则真实影响全部批次时，才允许全量重跑。
- LLM 批次必须区分可处理的核心内容和只读前后文，保存稳定批次 ID、上游指纹、尝试次数、模型用量与停止原因；汇合由程序校验完整覆盖、唯一性、原顺序和引用一致性，禁止让模型自由拼接整份结果。
- 本地纯函数、固定数据测试和不计费的资源任务可按需要反复执行。付费流程稳定后只做一次必要的完整正式验收；不得用多次全量付费运行代替定点诊断。

## 变更纪律

- 优先小批次、可验证、可回退的改动；先用测试锁定行为，再迁移调用方，最后删除旧路径。
- 遇到复杂算法、长流程编排、状态机或跨模块架构时，先调研成熟开源项目、论文和头部厂商的最佳实践，确认其适用边界、许可证、维护成本与验证方法；优先借鉴经过验证的设计，只有现有方案不适用时才自行实现。
- 处理模型驱动或结果不稳定的问题时，要主动展开解法空间，分别检查输入与上下文、提示词策略、模型与思考档位、输出协议、分批方式、程序校验和流程编排；用固定快照做最小对比实验，只有取得稳定提升质量或效率的证据后才进入正式流程。
- 当同一模型结果连续暴露多个不同的内容错误，或修复器已经需要多轮、跨句搬移、不断新增例外才能收敛时，应判定为上游生成方式或输入契约问题，停止继续堆叠终审规则。先调整分块、锚点、上下文边界、输出协议或流程职责，再保留少量只检查完整性、顺序、引用、时间和数据安全的不变量校验。
- 审核节点用于发现问题和提供质量证据，不得承担重新编排正文、反复修补模型输出或用题材专属规则把错误结果强行改到通过。能够由输入结构和生成约束预防的问题，必须在上游预防；审核默认给出 warning 或明确问题清单，只有数据损坏、内容缺失、顺序破坏、引用错误、时间非法、无依据新增等无法安全交付的通用问题才可阻断。
- 不顺手做无关重构、全局格式化或大面积改名。
- 不使用破坏性 Git 命令，不删除或覆盖用户真实数据。
- 架构改动要先确认依赖方向和状态所有权，不能为了局部方便制造反向依赖。

## 通用化与模型质量

- 产品能力默认面向未知但合法的输入。不得把测试素材中的人物、产品、题材、措辞、
  误译案例或一次性修正写进生产提示词、业务规则、默认配置或兜底代码。
- 固定提示词只描述跨内容类型仍成立的目标、边界和输出协议；当前内容专属信息必须
  来自该任务的版本化输入、已确认配置或动态上下文，并保持最小必要。
- 当前视频本土化优先保证英语到简体中文，但语言方向必须继续通过 typed contract
  和配置传递。新增核心契约与算法不得无必要写死语言；目标语言专属的自然度、
  标点和朗读规则应留在可替换的语言配置或适配边界。
- 修改模型提示词、输入投影、模型策略或质量判定前，必须先固定已验收基准，并用
  至少一个不同内容类型的固定快照做最小对比。基准质量下降、跨内容收益不明确或
  只能依赖样例补丁时不得进入正式流程。
- 优先删除不通用的假设或缩小输入，不得通过不断追加题材判断、特殊词表、审核门槛
  和结果后处理补丁掩盖上游问题。极特殊内容在真实需求出现后通过显式配置或独立
  策略定点扩展，不污染默认通用路径。

## 验证与收尾

- 按影响范围运行定点测试、相关全量测试、类型/静态检查和生产构建。
- 已经通过验收的行为必须先有自动回归保护，再扩展相邻功能；禁止只验证新增分支，不验证既有任务详情、字幕显示、播放器和状态持久化。
- 修改视频本土化领域、接口或页面时，开发中运行 `scripts/verify_video_localization_regression.sh --quick`，收尾运行不带参数的完整门禁。门禁只用固定测试数据，不得调用真实 ASR、LLM、搜索、识图或其他计费服务。
- Python 测试使用仓库 `.venv`，避免绕过项目运行环境。
- 任何能在 Web 端呈现或触发的功能，无论主要改动位于前端还是后端，收尾前都必须在最新构建和真实服务实例上完成 Web 端到端验证；不得只用单元测试、API、数据库或文件结果代替。这里的“真实服务实例”是指不替换业务实现，不代表允许使用用户正式数据库或现存项目；除非用户明确指定，验收服务仍必须使用本次运行独占的临时数据根目录。
- Web 端到端验证至少覆盖真实业务入口的任务触发或生成、运行中与历史状态、结果详情或错误提示，以及刷新后的持久可见性。测试项目和测试产物必须保存在验收临时根目录并在结束后清理。暂时没有网页触发入口时，可以通过公共 API 启动开发任务，但仍必须在 Web 页面核对用户可见结果，并明确记录入口限制。
- 只有功能本身无法在 Web 页面呈现或操作时才可跳过浏览器回归；收尾说明必须写明原因和替代验证证据。
- UI 改动还必须检查目标页面交互、响应式和控制台错误。
- 未取得新的验证证据前，不宣称问题已修复或任务已完成。
- 提交前检查 `git diff --check`、Git 状态和实际 diff，只提交本任务相关文件。

## 文档维护

- 架构边界、公共入口、状态所有权或版本化契约变化时，必须在同一提交更新架构活文档或领域 README。
- `docs/architecture/SYSTEM_ARCHITECTURE.md` 只写当前事实、长期原则和仍存在的过渡边界。
- 领域细节只放在领域 README 或契约；其他文档通过链接引用，不复制规则。
- 过程报告、一次性计划和日期审计不是当前实现依据。禁止在活文档写代理型号、阶段流水账、代码行数和会迅速过期的测试数量。
- 维护 `docs/DEV_STATUS.md`：它记录当前焦点、已完成工作、关键经验、协作偏好和待办，供新会话快速接手。**每个任务收尾时同步更新**：新结论写进「关键经验」，做完的条目从「待办」移除，过时内容直接删除，已领域化的内容改成链接。不写日期、提交号和测试数量，只保留仍然有效的信息。
